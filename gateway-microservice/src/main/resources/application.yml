spring:
  cloud:
    gateway:
      mvc:
        routes:
          - id: posts-route
            uri: ${POST_ROUTE_URI:http://localhost:8081}
            predicates:
              - Path=/posts/**
            filters:
              - PrefixPath=/api
              - AddResponseHeader=X-Powered-By,DanSON Gateway Service
              -