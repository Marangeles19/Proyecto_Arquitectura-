server:
  port: ${PORT:${SERVER_PORT:0}}
spring:
  application:
    name: ventafisica-service
    zipkin: http://localhost:9411
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:mysql://localhost/ventaDB
    username: root
    password: root

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
    instance:
      instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

#url APIS
stock:
  service:
    url: http://localhost:8080/api/stock